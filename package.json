{
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs --production"
  },
  "devDependencies": {
    "esbuild": "^0.18.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@google/genai": "^1.5.0"
  }
}
